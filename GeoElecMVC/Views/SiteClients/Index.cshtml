@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<GeoElecMVC.Models.SiteClients>

@{
    ViewData["Title"] = "Index";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create">Create</a>
</p>
<table class="table">
    <tr>
        <th>
            @* @Html.DisplayNameFor(model => model.UserName) *@
            @Html.Raw("<span>Username</span>")

        </th>
        <th>
            @* @Html.DisplayNameFor(model => model.Name) *@
            @Html.Raw("<span>Company</span>")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Company)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserName">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.UserName" onclick="return confirm('Are sure wants to delete?');">Delete</a>
            </td>
        </tr>
    }
</table>