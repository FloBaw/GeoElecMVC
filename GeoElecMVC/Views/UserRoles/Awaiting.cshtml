@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<GeoElecMVC.Models.UserRoles>

@{
    ViewData["Title"] = "Awaiting";
}

<h2>@ViewData["Title"]</h2>

<table class="table">
    <tr>
        <th>
            @* @Html.DisplayNameFor(model => model.UserName) *@
            @Html.Raw("<span>Username</span>")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                <a asp-action="EditAwaiting" asp-route-id="@item.UserName">Edit</a> |
                <a asp-action="DeleteAwaiting" asp-route-id="@item.UserName" onclick="return confirm('Are sure wants to delete?');">Delete</a>
            </td>
        </tr>
    }
</table>

<div>
    <a asp-action="Index">Back to List</a>
</div>